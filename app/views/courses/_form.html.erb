<%=form_for @course do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %><br />

    <%= f.label :professor_id %>
    <%= f.select :professor_id, Professor.active.collect {|p| [ p.name, p.id ] } %><br />

    <%= f.submit %>

<%end%>
